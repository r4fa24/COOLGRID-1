/**
 * Key-free vector basemap (OpenFreeMap "Positron", OpenStreetMap data). Chosen
 * for its pale, airy look so the simulated heat zones stay the visual hero.
 */
export const BASEMAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/positron'
